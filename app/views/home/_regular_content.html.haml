-if @cu.present? && @cu.is_owner == false
  .row
    .large-4.columns
      #button{style:"text-align:center"}
        / =link_to("Check-in!", "#", :class => "button radius large expand", )
        =link_to(image_tag("checkin2.png"), "#", :class => 'login', :id => "checkin_btn")
    .large-4.columns
      =link_to(image_tag("create_crawl_coaster2.png"), new_pub_crawl_path, :class => 'login', :remote => true)
    .large-4.columns.login
      / = link_to("All Crawls", pub_crawls_path, :class => "button radius large expand", :remote => true)
      =link_to(image_tag("allcrawls2.png"), pub_crawls_path, :class => 'login', :remote => true)
  .row
    .large-12.columns
      #reg_content

