.row
  .small-12.columns#mobile_locations
    -if @venues.present?
      %p My sources say you are at one of the following venues. Select which one:
      .row
        .small-10.small-offset-2.columns#mobile_locations_list
          %ul
            -@venues.each do |venue|
              %li
                .location
                  %a{href:"/check_ins/select_location?venue=#{venue.id}", "data-remote" => "true"}= venue.name

    -else
      %h1 We Couldn't Find You. Try Again?
      =link_to("Check-in!", "#", :class => "button radius large expand", :id => "checkin_btn")