%h3 Enter the email addresses of all the friends you'd like to invite (separated by commas):
=form_tag invite_friends_pub_crawls_path, :remote => true do
  =hidden_field_tag :crawl, @crawl.id
  =text_area_tag :emails
  =submit_tag nil, :class => "button round small"